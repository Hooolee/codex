# Fork 版单 Provider 多 Model 调度方案

## Summary
目标是把你的 fork 做成“主线程模型固定不动，成本优化通过 subagent 分工完成”的版本，同时尽量降低后续同步官方 upstream 的冲突成本。

第一版实现范围：

- 主线程从头到尾保持当前 model，不做自动切换
- 自定义 agent 可声明自己的 `model` 与可选 `model_reasoning_effort`
- skill 在派生 subagent 时可基于用户全局“模型画像”自动选择更合适的 model
- 所有 model 都来自同一个 provider 的可用模型列表
- `tool choice` 这次不实现独立模型调度，只在架构上预留未来扩展位

硬性兼容原则：

- fork 用户配置根目录只是官方 `~/.codex/` 的等价替代根目录，仅更换目录名
- 除 fork 新增文件外，用户级目录布局、职责和语义尽量与官方保持一致
- 默认行为不变，新增能力全部为可选增强

## Implementation Changes
### 1. Fork 专属用户配置根目录
- 为 fork 定义一个固定的新用户级 home/config 根目录，替代官方默认 `~/.codex/`。
- 该根目录是官方用户级目录的平移替身，继续承载与官方相同类型的用户级配置与状态：
  - `config.toml`
  - `agents/`
  - 其他当前依赖 `codex_home` 的用户级状态
- fork 仅在此基础上新增模型画像文件，不改变其他既有职责。
- 项目级 `.codex/` 机制保持兼容，不改目录名。
- 不做双读 `~/.codex/` 与 fork 目录，也不做自动迁移；第一版以 fork 根目录为唯一用户级真源。

### 2. 自定义 agent 的 model 配置
- 继续复用现有 `agents/*.toml` 机制，不新增新格式。
- 支持的来源：
  - fork 用户级 `<fork_home>/agents/*.toml`
  - 项目级 `<repo>/.codex/agents/*.toml`
- 在 agent 文件中正式允许写：
  - `model`
  - `model_reasoning_effort`
- built-in agent 保持现有逻辑，不新增默认 model 配置。
- 模型解析优先级固定为：
  - `spawn_agent(model=...)`
  - 自定义 agent 文件中的 `model` / `model_reasoning_effort`
  - 父线程继承
- `fork_context = true` 的 full-history fork 继续禁止 model override，不改变现有语义。

### 3. 用户全局模型画像
- 在 fork 用户级根目录下新增结构化 TOML 画像文件，作为个人模型调度配置。
- 文件按“模型能力画像”建模，而不是普通会话配置，至少包含：
  - model 标识
  - task tags / strengths
  - 可选 `cost_tier`、`speed_tier`、`notes`
  - `generated_at` 或版本信息
- 新增一个显式触发的“模型评估”入口：
  - 读取当前 provider 下可用模型列表
  - 由当前主模型基于这些模型生成画像内容
  - 写入用户级画像文件
- 画像允许手工编辑；运行时仅把它当作偏好路由规则，不做绝对真理。

### 4. Skill 派生 subagent 的自动选型
- 扩展 skill `agents/openai.yaml`，新增 task tags 元数据字段。
- skill 不直接硬编码 model，只表达任务属性。
- 第一版 skill 自动选型仅作用于“派生 subagent”，不影响主线程模型。
- 路由流程固定为：
  - skill 声明 task tags
  - 调度器读取用户级模型画像
  - 在当前 provider 的可用模型中寻找匹配项
  - 命中则作为 subagent 的 model override
  - 未命中或画像不可用则回退到父线程 model
- 第一版使用少量稳定标签即可，例如：
  - `research`
  - `coding`
  - `review`
  - `classification`
  - `fast-cheap`
  - `deep-reasoning`

### 5. Resolver 组织方式
- 将 fork 增强逻辑集中成少数独立模块，避免散落修改：
  - `agent_model_resolver`
  - `model_profile_loader`
  - `skill_model_matcher`
- `spawn_agent` 主流程只负责调用 resolver，不内嵌复杂选型规则。
- skill 派生子代理时统一经过 matcher，避免未来多处逻辑漂移。

### 6. 可观测性与降级
- 为 subagent spawn 相关事件补充可追踪信息：
  - 是否由 skill 路由触发
  - 选中了哪个 model
  - 命中了哪条画像规则
  - 是否发生 fallback，以及原因
- 路由失败统一安全降级为继承父线程 model，不阻断主流程。
- 没有画像、标签非法、目标 model 不存在、model 当前不可用、reasoning 不兼容时，都走同一回退路径。

### 7. Tool choice 的未来扩展位
- 这次不实现独立 `tool choice model`。
- 但在设计上保留一个未来扩展方向：
  - 当前 tool 调用仍保持官方机制，即每轮将“当前对模型可见的工具 schema/spec”发给主模型，由主模型决定是否直接答复或发起 tool call。
  - 未来若要做 cheap tool-choice model，可在“构建工具可见集之后、主模型实际生成 tool call 之前”增加一个可选 planner 阶段。
- 本次实现不要提前改主执行链，只要求新的 resolver 设计不要把“model 选择”写死在 subagent 专用路径里。

## Public Interfaces / Config Additions
- fork 用户配置根目录：作为本 fork 默认 `codex_home`
- 自定义 agent TOML：正式支持
  - `model`
  - `model_reasoning_effort`
- skill `agents/openai.yaml`：新增 task tags 字段
- fork 用户根目录：新增模型画像 TOML 文件
- 新增显式模型评估入口，用于生成/刷新模型画像

## Test Plan
- fork 启动后，用户级配置与 agent 发现来自 fork 用户目录，而非 `~/.codex/`
- 除目录名变化外，fork 用户目录下原有用户级配置语义与官方一致
- 项目级 `.codex/agents/*.toml` 仍可被发现并正常参与层叠
- 自定义 agent 配置 `model` 时，spawn 的子 agent 使用该 model
- 显式 `spawn_agent(model=...)` 时，覆盖 agent 文件默认 model
- built-in agent 行为与当前一致
- full-history fork 仍拒绝 model override
- 无画像文件时，skill 派生 subagent 回退到父线程 model
- skill 有标签且画像命中时，派生 subagent 使用匹配到的 model
- 画像指向不可用或不可见 model 时，安全回退
- skill 标签缺失、为空、非法时，加载或路由有明确降级行为
- 模型评估入口能基于当前可用模型列表生成画像，并可再次读取使用
- spawn 事件或状态中可看到选型来源与 fallback 原因

## Assumptions
- 主线程 model 永远不自动切换，所有降本都通过 subagent 分工完成
- 第一版只支持单 provider 下多 model，不支持 provider 切换
- 项目级 `.codex/` 保持兼容，以降低后续 merge 官方更新的成本
- 模型画像是用户个人资产，只放在 fork 用户级目录，不放项目级 `.codex/`
- skill 通过任务标签表达偏好，不直接写死 model
- `tool choice` 本次只记录未来扩展方向，不进入第一版实现
